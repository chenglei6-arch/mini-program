# 复制为 .env 后只填写本地值，生产环境应由部署平台注入。
DB_NAME=mini_program
DB_USERNAME=mini_program_app
DB_PASSWORD=replace_with_a_strong_password
MYSQL_ROOT_PASSWORD=replace_with_another_strong_password
MYSQL_PORT=3307
DB_URL=jdbc:mysql://localhost:3307/mini_program?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false

SERVER_PORT=8080
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# 微信小程序登录：生产必须注入正式 appid/secret；两者为空时登录会直接失败。
# 本地联调若拿不到微信凭据，显式填下面的 dev openid 才会走开发登录（不填就不放行）。
WECHAT_APPID=
WECHAT_SECRET=
APP_AUTH_DEV_OPENID=
# 登录会话有效期（秒），到期后小程序会自动重新 wx.login。
AUTH_ACCESS_TOKEN_TTL=7200

# 阿里云 OSS（v2 SDK）：app.oss.* 的注入来源，密钥不要写进 application.yml。
# OSS_ACCESS_KEY_ID / OSS_ACCESS_KEY_SECRET 留空时，v2 会直接读同名环境变量。
OSS_ENDPOINT=oss-cn-beijing.aliyuncs.com
OSS_REGION=cn-beijing
OSS_BUCKET=replace_with_your_bucket
OSS_ACCESS_KEY_ID=
OSS_ACCESS_KEY_SECRET=
# 文件访问公网地址（bucket 虚拟主机形式），留空则按 https://{bucket}.{endpoint} 推导。
OSS_PUBLIC_BASE_URL=
