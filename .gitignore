# 根级忽略：只覆盖两个子项目之外的公共内容。
# 各子项目另有自己的 .gitignore：
#   mini-program-frontend/.gitignore
#   mini-program-backend/.gitignore

# 设计源文件归档（约 662MB：分层工程文件、部件 PNG、设计说明、交付压缩包）
# 只做归档与二次设计用，不参与构建与发布
/design-sources/

# 分层设计文件兜底：任何落进仓库的 PSB/PSD/AI 都不该入库
*.psb
*.psd
*.ai

# git 历史备份（体积大，只做本地兜底，勿入库）
*.bundle

# 编辑器 / 系统
.idea/
.vscode/
.DS_Store
Thumbs.db
